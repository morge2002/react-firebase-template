// These are credentials for a Firebase project. They are not sensitive, so it's ok to have them here.
const firebaseConfig = {
		apiKey: "",
		authDomain: "",
		projectId: "",
		storageBucket: "",
		messagingSenderId: "",
		appId: "",
		measurementId: ""
	};

export {firebaseConfig}